# E2E Test Applications Configuration
# Format: PACKAGE_NAME|SOURCE_URL|DESCRIPTION
# Lines starting with # are comments
#
# These applications are from the jDeploy homepage and other known good examples.
# Each application will be tested for headless install, verification, uninstall, and verification.
#
# Note: SOURCE_URL can be empty if the package is published to npm/jdeploy registry.
# The install script will use the package name format: https://www.jdeploy.com/~{package}/install.sh

# jDeploy Demo Applications
# Note: jdeploy-demo-swingset2 URL serves jdeploy-demo-swingset3 package (historical redirect)
jdeploy-demo-swingset3||SwingSet3 - canonical Swing demo app

# Community Applications (uncomment to test more apps)
# snapcharts||SnapCharts - Chart design app
